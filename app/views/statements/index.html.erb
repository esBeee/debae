<%= render 'add_to_history', name: t("history.statements.index", count: (params[:page].to_i == 0 ? 1 : params[:page].to_i), default: "Statements") %>

<section class="row index-headline">
  <h1><%= t(".headline") %></h1>
</section>

<section class="row">
  <%= render @statements %>
</section>

<%= will_paginate @statements %>
