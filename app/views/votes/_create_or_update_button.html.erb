<%= form_for Vote.new(voteable: voteable, is_pro_vote: is_pro_vote) do |f| %>
  <%= f.hidden_field :voteable_id %>
  <%= f.hidden_field :voteable_type %>
  <%= f.hidden_field :is_pro_vote %>
  <%= submit_tag submit_value %>
<% end %>
