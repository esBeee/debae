# Translations for ActiveRecord model names and its attributes.
# Learn more at http://guides.rubyonrails.org/i18n.html#translations-for-active-record-models
#
# Also includes additional error messages (for custom validations). You can have a look at the included ActiveModel
# error messages at https://github.com/rails/rails/blob/master/activemodel/lib/active_model/locale/en.yml.

de:
  activerecord:
    errors:
      messages:
        statement_eq_argument: "kann nicht mit sich selbst argumentiert werden"
        statement_is_argument_for_argument: "ist bereits als Argument für das Argument deklariert"
